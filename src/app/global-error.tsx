"use client";

import ErrorReporter from "@/components/ErrorReporter"; // Custom error reporting/logging component

// Entry point for Next.js error boundary page (app/error.tsx)
// Automatically handles runtime errors in the app router segment + nested children
export default ErrorReporter;
